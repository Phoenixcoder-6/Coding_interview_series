1. String Compression.  => aacc-> a2c2
2. Reverse words in a sentence.
3. Check if two strings are anagrams or not.
4. Find the first non-repeating character in a string
5. Find the longest palindrome from the given list.
6. Check if a string contains only unique characters.
7. Find the kth smallest element in an array (QuickSelect / Sorting)
8. Merge two sorted arrays without extra space(Using Gap Method)
9. Sort an array of 0s, 1s, and 2s (Dutch National Flag problem)
10. Find the missing number in an array of size N-1.
11. Find the majority element in an array (Appears more than n/2 times).
12. Generate all permutations of a string.
13. Find all subsets of a given set.
14. How does a binary search tree (BST) work? 
15.
16.
